# Scope
# Namespace - Mutate pods in the release namespace only & enforce NetworkPolicy
# Cluster - Mutate pods in all namespaces
scope: Cluster

server:
  image:
    repository: ghcr.io/kubepmix/kubepmix
    tag: 0.0.12
  webhook:
    port: 8443
  pmix:
    port: 3333
    interface: eth0
